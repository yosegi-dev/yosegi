node_modules/
dist/
.yosegi/
.pack/
*.log
.DS_Store

# The source of truth for skills/ is the repository root. packages/server/skills is a copy for
# publishing, so it is not tracked.
packages/server/skills/

# brand asset generation (font is fetched, not committed)
assets/brand/*.ttf

# Agent worktrees are full checkouts of this repository. Only worktrees/ is ignored, so that
# anything else under .claude/ stays available to commit.
.claude/worktrees/

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/
docs/public/
