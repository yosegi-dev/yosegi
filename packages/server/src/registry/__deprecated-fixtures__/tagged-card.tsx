// Fixture for reading Storybook's "deprecated" tag through index.json.

/** A card whose Story carries tags: ["deprecated"]. */
export function TaggedCard({ title }: { title: string }) {
	return <div>{title}</div>;
}
